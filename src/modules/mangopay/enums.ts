export enum MangopaySortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum MangopayUserCategory {
  CUSTOMER = 'PAYER',
  HERO = 'OWNER',
}

export enum MangopayPersonType {
  PROFESSIONAL = 'LEGAL',
  INDIVIDUAL = 'NATURAL',
}

export enum MangopayLegalPersonType {
  BUSINESS = 'BUSINESS',
  PARTNERSHIP = 'PARTNERSHIP',
  ORGANIZATION = 'ORGANIZATION',
  SOLETRADER = 'SOLETRADER',
}

export enum MangopayCurrency {
  EUR = 'EUR',
}

export enum MangopayTransactionType {
  PAYIN = 'PAYIN',
  PAYOUT = 'PAYOUT',
  TRANSFER = 'TRANSFER',
}
